CREATE DATABASE IF NOT EXISTS test;

-- 필요시 별도의 테스트 사용자 생성 (선택사항)
-- CREATE USER IF NOT EXISTS 'test_user'@'%' IDENTIFIED BY 'test_password';
-- GRANT ALL PRIVILEGES ON commerce_test.* TO 'test_user'@'%';
-- FLUSH PRIVILEGES;

